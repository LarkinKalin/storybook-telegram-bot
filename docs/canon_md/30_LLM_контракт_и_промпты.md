============================================================
ПРИЛОЖЕНИЕ L. Runtime-параметры и контракт генерации (MVP v0.1)
============================================================

Контракт формата (intro_3p_plus_choice):
- 3 абзаца вступления (цельный рассказ)
- затем один выбор:
  А) ...
  Б) ...
  В) ...
  Свой вариант: ______

Runtime schema (минимум):
- theme_id (обяз.)
- style_id (опц.; если не задан — theme.style_default)
- protagonist_mode: kid | known | auto
- content_flags (глобальные + тема)
- format_contract: intro_3p_plus_choice (фиксировано)

Правила стилизации "в духе автора":
- это имитация манеры (голос, ритм, лексика), но НЕ копирование текста
- запрет: прямые цитаты/узнаваемые сцены/фрагменты оригинальных произведений

starter_prompt (starter_brief) — универсальный:
- это brief-инструкция, чтобы каждый запуск давал новое вступление
- не хранить в теме конкретную сцену, только якоря мира и требования к структуре вступления

SOURCE OF TRUTH (репозиторий):
- docs/GEN_CONTRACT.md — нормативный контракт
- content/themes.yaml — темы
- content/styles.yaml — стили
- content/tag_vocab.yaml — словарь тегов


PATCH — 30_LLM_контракт_и_промпты — WHY_TEXT (“🧠 Почемучка”)
30.why.0 — Назначение
WHY_TEXT (“🧠 Почемучка”) — режим быстрых ответов на вопросы. Пользователь задаёт вопрос текстом, бот отвечает понятными словами.
Важно: WHY_TEXT не вмешивается в активную сказку (ACTIVE-сессия не меняет step; это PAUSE-поведение).
________________________________________
30.why.1 — Триггер вызова
WHY_TEXT активируется из L1 кнопкой 🧠 Почемучка.
Внутри WHY_TEXT:
•	любое message.text трактуется как вопрос и уходит в механизм ответа WHY_TEXT,
•	любые non-text апдейты НЕ считаются вопросом и НЕ вызывают механизм ответа (см. 30.why.6).
________________________________________
30.why.2 — Входные данные (контракт)
Минимальный набор данных для ответа:
•	question_raw: исходный текст пользователя
•	mode_read: kid | adult (из настройки st:read)
•	lang: ru
•	context_optional (опционально):
o	last_topic: последняя выбранная тема (если есть, только для подсказки контекста)
o	active_exists: bool (для UI-логики, но не для ответа по сути)
________________________________________
30.why.3 — Выходные данные (контракт качества)
Ответ — plain text, одним сообщением.
Обязательные свойства:
•	Понятность:
o	без терминов; если термин неизбежен, 1 короткое объяснение простыми словами
•	Структура:
o	1 аналогия/пример обязательно
o	1 мягкая шутка обязательно (без издёвки над ребёнком)
•	Длина:
o	kid: 4–6 предложений
o	adult: 6–10 предложений
•	Тон:
o	лёгкий озорной рассказчик “в духе Носова”: живо, просто, без морализаторства и занудства
Запрещено:
•	токсичность, унижение, запугивание
•	взрослые темы (18+), подробное насилие, самоповреждение и т.п.
•	“Я ИИ/модель/нейросеть” (в WHY_TEXT это лишний мусор)
________________________________________
30.why.4 — Механика ответа (MVP v0.1 — без LLM)
Источник истины: локальная база Q/A (json) + детерминированный матчинг.
30.why.4.1 — Нормализация вопроса
•	lower()
•	trim
•	“ё” → “е”
•	убрать пунктуацию
•	схлопнуть пробелы/переносы в один пробел
30.why.4.2 — Схема базы Q/A (минимум)
Файл: why_qa.json (локально, read-only)
Запись:
•	id: string (unique)
•	keywords: string[] (фразы/слова)
•	answer_kid: string
•	answer_adult: string
•	priority: int (default 0)
30.why.4.3 — Детерминированный матчинг
Для каждого entry:
•	phrase_match = true, если любая keyword-фраза встречается подстрокой в нормализованном вопросе
•	token_score = кол-во токенов, совпавших с токенами вопроса
score:
•	если phrase_match: 100 + token_score
•	иначе token_score
Выбор:
•	максимальный score
•	tie-break: priority выше → затем id лексикографически
•	порог: если score < 2 → ответ “не найден”
30.why.4.4 — Ответ по базе
•	если найдено:
o	kid → answer_kid
o	adult → answer_adult
•	если не найдено → fallback (см. 30.why.5)
•	состояние WHY_TEXT сохраняется (не выходим автоматически)
________________________________________
30.why.5 — Fallback (без LLM)
Fallback — это не выход в меню. Это “не уверен, уточни”, остаёмся в WHY_TEXT.
kid:
“Я не уверен. Попробуй спросить по-другому: ‘почему …?’, ‘как работает …?’, ‘что будет если …?’. Про что именно ты хочешь узнать: про A или про B?”
adult:
“Не уверен. Уточни формулировку: ‘почему …?’, ‘как устроено …?’, ‘что будет если …?’. Уточни контекст: где/когда/в чём именно.”
Если база Q/A не загрузилась/битая:
•	используем fallback по mode_read
•	логируем why.qa_load_error
________________________________________
30.why.6 — Non-text апдейты (без LLM)
Если пользователь прислал voice/audio/sticker/photo/video/document:
Ответ (фиксированный):
“Понимаю только текстовый вопрос. Продиктуй через диктовку так, чтобы отправилось текстом, или напиши руками.”
Остаёмся в WHY_TEXT.
________________________________________
30.why.7 — LLM-контур (будущая замена MVP v0.1)
Когда подключаем LLM, WHY_TEXT использует тот же UX и тот же “контракт качества”, меняется только источник ответа:
•	Сначала пробуем Q/A (дешёвый и быстрый путь),
•	Если не найдено (score < 2) → вызываем LLM.
30.why.7.1 — LLM Prompt (system)
SYSTEM (фиксированный):
Ты объясняешь сложное простыми словами. Отвечай по-русски.
Правила:
•	mode_read определяет длину: kid=4–6 предложений, adult=6–10.
•	Обязательно: 1 аналогия/пример и 1 мягкая шутка.
•	Без токсичности, без взрослого контента, без запугивания.
•	Не говори “я ИИ/модель”.
•	Пиши простым человеческим языком, без терминов; если термин нужен, объясни одним простым предложением.
30.why.7.2 — LLM Prompt (user)
USER:
mode_read: {kid|adult}
question: {question_raw}
Ответ: plain text, без списков и без markdown.
________________________________________
30.why.8 — Логи (минимум)
Не логировать полный текст вопроса. Допустимо:
•	длина
•	sha256(normalized_question)
События:
•	why.q_received(mode, len, hash)
•	why.q_matched(id, score)
•	why.q_notfound
•	why.qa_load_error
•	why.llm_called (когда появится LLM)
________________________________________
30.why.9 — DoD WHY_TEXT (готовность)
Готово, если:
•	без LLM: Q/A работает детерминированно + fallback + non-text обработан
•	ответы соблюдают длину и правила “пример + мягкая шутка”
•	режим не ломает ACTIVE-сказку (никаких изменений step/status)
•	выход только через 🏠 Меню (go:l1), остальное остаётся внутри WHY_TEXT

30.PATCH.TG.7.3+TG.7.4 — MODEL SPLIT: KIMI(TEXT) + FLUX(IMAGE) (Canon v1.0)
Статус: ACTIVE
Дата: 2026-01-28
Область: LLM contract / runtime routing / image pipeline bridge

1) Канон провайдеров и моделей (на сегодня)
- Текст (story_step/story_final): OpenRouter → moonshotai/kimi-k2.5
- Изображения (иллюстрации): OpenRouter → black-forest-labs/flux.2-pro
- OpenAI/MiniMax в контуре MVP не используются (считаются нецелевыми по качеству/стабильности).

2) Разделение контуров (строго)
A) Текстовый контур:
Engine/Runtime → Kimi: отправляем story_request (JSON-only) в messages.user.content
Kimi → Engine/Runtime: получаем story_step/story_final (JSON-only), валидируем, применяем engine.

B) Контур иллюстраций:
Runtime → FLUX: отправляем prompt для изображения (+ опционально reference image)
FLUX → Runtime: получаем изображение (base64 data URL или аналог), сохраняем как asset, отправляем в TG.

Важно: текстовая модель НЕ “рисует” и НЕ возвращает картинки. Она возвращает только текст и данные-мосты для иллюстрации.

3) Поле-мост для картинки (добавление к story_step/story_final)
В каждом валидном ответе story_step и story_final допускается поле:
- image_scene_brief: string | null

Назначение:
- краткое описание сцены для генерации иллюстрации (1–3 предложения),
- без метаданных движка,
- без повторов всей истории,
- НЕ содержит персональных данных,
- язык: русский.

Правило: наличие image_scene_brief НЕ означает обязательную генерацию картинки.
Решение “генерим ли картинку на этом шаге” принимает runtime по расписанию engine (см. 20-й документ).

4) JSON-only и reasoning
- Для сказки reasoning выключен. Никаких reasoning_details в прод-генерации step/final.
- Контракт остаётся JSON-only. Любые “объяснения”, “комментарии”, “markdown” считаются невалидными.

5) Retry/Fallback (сохраняется, усиливается для image pipeline)
- На текстовом контуре: retry=1. Если оба фейла → fallback step/final (согласно политике отказов).
- На контуре картинок: retry=1. Если оба фейла → отправляем шаг без изображения (текст + кнопки), игра продолжается.
Картинка НЕ имеет права блокировать L3 и не должна приводить к soft-lock.

6) Ограничения расходов (MVP guardrails)
Runtime обязан ограничивать:
- input clamp (по символам) для текста, чтобы не улететь в стоимость,
- max_tokens отдельно для story_step/story_final,
- timeout на запросы текст/картинки.

7) Пакет на изображение (внутренний runtime-конверт, не LLM контракт)
После получения story_step/story_final runtime формирует внутренний пакет IMG_REQUEST (пример):
{
  "req_id": "...",
  "step_ui": 4,
  "mode": "t2i|i2i",
  "prompt": "...",
  "reference_asset_id": "<asset_id step 1>" | null,
  "size": "512x768",
  "model": "black-forest-labs/flux.2-pro"
}
Далее этот пакет конвертируется в запрос OpenRouter под конкретный image spec.

============================================================
TG.7.4.01 — Image Provider v0.1 (FLUX.2 Pro) — UX/Delivery Rule
============================================================

1) Расписание иллюстраций (детерминированно)
- 8 шагов: 1/4/8
- 10 шагов: 1/4/8
- 12 шагов: 1/5/9

2) Reference-цепочка
- step 1: t2i без reference → сохраняем как role=reference
- step 4/8 (или 5/9): i2i с reference изображения step 1 → role=step_image, reference_asset_id=asset(step1)

3) Delivery в Telegram (MVP)
- Сообщение шага: текст + кнопки отправляются сразу.
- Иллюстрация отправляется асинхронно отдельным сообщением (reply к сообщению шага).
- Если генерация упала: retry=1, затем пропуск иллюстрации. Прохождение не блокируется.

4) DB/Storage
- base64 в Postgres не пишем
- только assets(storage_key, sha256, bytes, mime, w/h) + session_images(step_ui, role, reference_asset_id, image_model, prompt)

============================================================
LLM runtime contract (факт)
============================================================

1) Что отправляем
- messages[]: строго два сообщения.
  - system: канонический промпт (JSON-only, правила, стиль).
  - user: JSON story_request (без markdown).
- request параметры: model, response_format, temperature, max_tokens, reasoning, plugins.

2) Что получаем
- JSON-only ответ:
  - story_step: {"text":"...","recap_short":"...","choices":[{"choice_id":"A","label":"..."}]}
  - story_final: {"text":"..."}
- Кнопки в Telegram строятся из parsed_json.choices (label + choice_id).

3) Связность и состояние (факт)
- Вход story_request включает recaps (последние 3–5), last_choice и state (traits/milestones).
- recap_short обязателен в каждом story_step и сохраняется для следующего шага.

3) Отладка и дампы
- Дампы: var/llm_dumps/*.json (на хосте: /srv/git/skazka/var/llm_dumps).
- Просмотр: apps/tg-bot/scripts/llm_dump_show.sh (или llm_dump_show.py).
